# API Dokümantasyonu
Bu modül, `BotDetectionAnalyzer` sınıfı üzerinden temel fonksiyonları sunar:
- `analyze_site(url, report_name)`: Hedef siteyi analiz eder.
- `calculate_risk_score(fingerprint)`: Analiz verilerini puanlar.
- `generate_report()`: Sonuçları JSON ve TXT olarak kaydeder. 
